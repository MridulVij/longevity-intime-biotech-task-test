import 'package:flutter_test/flutter_test.dart';

void main() {
  // Each Testing Function for Each Class which contains Logical part
  test('just simple number verifying', () {
    final val = 1;
    expect(val, 1);
  });
}
